import { FilterIssuesPipe } from './filter-issues.pipe';

describe('FilterIssuesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIssuesPipe();
    expect(pipe).toBeTruthy();
  });
});
